///
/// HybridSpineViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridSpineViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::spine::views {

  extern const char HybridSpineViewComponentName[] = "SpineView";

  HybridSpineViewProps::HybridSpineViewProps(const react::PropsParserContext& context,
                                             const HybridSpineViewProps& sourceProps,
                                             const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    atlasName([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("atlasName", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.atlasName;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.atlasName);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SpineView.atlasName: ") + exc.what());
      }
    }()),
    skeletonName([&]() -> CachedProp<std::string> {
      try {
        const react::RawValue* rawValue = rawProps.at("skeletonName", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.skeletonName;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::string>::fromRawValue(*runtime, value, sourceProps.skeletonName);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SpineView.skeletonName: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::spine::HybridSpineViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<margelo::nitro::spine::HybridSpineViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("SpineView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridSpineViewProps::HybridSpineViewProps(const HybridSpineViewProps& other):
    react::ViewProps(),
    atlasName(other.atlasName),
    skeletonName(other.skeletonName),
    hybridRef(other.hybridRef) { }

  bool HybridSpineViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("atlasName"): return true;
      case hashString("skeletonName"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridSpineViewComponentDescriptor::HybridSpineViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  react::Props::Shared HybridSpineViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                      const react::Props::Shared& props,
                                                                      react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridSpineViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridSpineViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridSpineViewShadowNode&>(shadowNode);
    const HybridSpineViewProps& props = concreteShadowNode.getConcreteProps();
    HybridSpineViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::spine::views
